{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window — minimal permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ],
  "platforms": ["linux", "macOS", "windows"],
  "deny": [
    "fs:default",
    "shell:default"
  ]
}
